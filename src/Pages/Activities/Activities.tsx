import React, { Fragment } from 'react';

interface IActivitiesProps {
  [key: string]: any;
}

const Activities: React.SFC<IActivitiesProps> = (props) => <Fragment>Activities</Fragment>;

export default Activities;
